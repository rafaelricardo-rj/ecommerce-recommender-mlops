# ci-cd.yml — Pipeline de integração e entrega contínua.
#
# Dois jobs:
#   ci:     roda em todo PR e push — valida lint e testes.
#   deploy: roda apenas no merge à main — builda a imagem e sobe/atualiza o
#           MLflow Server na VPS.
#
# O treino dos modelos NÃO roda aqui: a VPS tem só 1 vCPU e o treino da MLP
# (até 300 épocas) estourava o timeout do job. O Registry é populado uma
# única vez rodando "src/train.py" localmente com MLFLOW_TRACKING_URI
# apontando para a URL pública da VPS — não precisa repetir a cada deploy.
#
# Segredos necessários (configurar em Settings → Secrets and variables → Actions):
#   VPS_HOST         → IP ou hostname da VPS
#   VPS_USER         → usuário SSH
#   VPS_SSH_KEY      → chave privada SSH (conteúdo do arquivo ~/.ssh/id_rsa)
#   VPS_PROJECT_PATH → caminho do projeto na VPS (ex: /home/usuario/ecommerce-recommender-mlops)

name: CI/CD

on:
  push:
    branches: [main]
    # Commits que só mexem em documentação não precisam rebuildar nem
    # redeployar o MLflow Server na VPS.
    paths-ignore:
      - "**.md"
      - "docs/**"
  pull_request:
    branches: [main]

jobs:
  # ---------------------------------------------------------------------------
  # Job 1: Integração Contínua
  # Roda em todo PR e push para garantir que o código está correto antes do merge.
  # ---------------------------------------------------------------------------
  ci:
    name: Lint e Testes
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código
        uses: actions/checkout@v4

      - name: Instalar uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Instalar dependências
        run: uv sync --all-groups

      - name: Lint (ruff)
        run: uv run ruff check .

      - name: Testes (pytest)
        run: uv run pytest

  # ---------------------------------------------------------------------------
  # Job 2: Deploy
  # Roda apenas quando há push direto na main (ou seja, após um PR ser aprovado
  # e mergeado). Depende do job ci — se os testes falharem, o deploy não acontece.
  # ---------------------------------------------------------------------------
  deploy:
    name: Deploy na VPS
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Executar pipeline na VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 15m
          # set -e: interrompe o script imediatamente se qualquer comando falhar.
          script: |
            set -e
            PROJECT_PATH="${{ secrets.VPS_PROJECT_PATH }}"
            REPO_URL="${{ github.server_url }}/${{ github.repository }}.git"

            echo "--- Verificando repositório ---"
            # Clona o repositório na primeira execução; nas seguintes, apenas atualiza.
            if [ ! -d "$PROJECT_PATH/.git" ]; then
              echo "Repositório não encontrado. Clonando..."
              git clone "$REPO_URL" "$PROJECT_PATH"
            fi
            cd "$PROJECT_PATH"
            git pull origin main

            echo "--- Configurando ambiente ---"
            # .env não é versionado; criado a partir do template na primeira execução.
            [ -f .env ] || cp .env.example .env

            echo "--- Construindo imagens ---"
            docker compose -f docker-compose.prod.yml build

            echo "--- Subindo/atualizando o MLflow Server ---"
            # up -d --force-recreate garante que o container use a imagem
            # recém-buildada mesmo que já estivesse rodando.
            docker compose -f docker-compose.prod.yml up -d --force-recreate mlflow-server

            echo "--- Deploy concluído ---"
